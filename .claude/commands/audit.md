Your Goal is to update any vulnarable dependencies

Do the following: 
1. Run `npm audit` to find the vulnarable installed packages in this project
2. Run `npm audit fix` to apply updates
3. Run tests and spin the server using `npm start dev` if needed to verify the updates didn't break anything.